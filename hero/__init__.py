#!/usr/bin/env python3

from hero.hero import find_ip, setup, init, cmd, render, setCanMoveAll, scan, scanx, testx, moveRight, moveLeft, moveUp, moveDown, canMoveRight, canMoveLeft, canMoveUp, canMoveDown
from hero.command_pb2 import CmdType, Cmd
